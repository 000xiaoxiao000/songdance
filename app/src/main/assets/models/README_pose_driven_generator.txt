Place the self-trained pose-driven image generation model here:

  pose_driven_generator.tflite

The app only uses the self-trained TFLite model for image generation.
Use tools/pose_model_factory to train/distill it.
